{
  "name": "stopots-clone-backend",
  "version": "0.0.1",
  "description": "stopots clone backend",
  "main": "index.js",
  "scripts": {
    "dev": "tsnd --transpile-only --ignore-watch node_modules src/server.ts",
    "dev2": "tsnd --clear --transpile-only --ignore-watch node_modules src/server.ts",
    "build": "tsc --build",
    "start": "node dist/server.js"
  },
  "keywords": [
    "stopots",
    "clone"
  ],
  "author": "Lucas8x",
  "license": "MIT",
  "dependencies": {
    "chalk": "^4.1.0",
    "dotenv": "^8.2.0",
    "socket.io": "3.1.0"
  },
  "devDependencies": {
    "@types/node": "14.14.22",
    "ts-node": "9.1.1",
    "ts-node-dev": "1.1.1",
    "typescript": "4.1.3"
  }
}
